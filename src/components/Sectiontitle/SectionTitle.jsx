import React from 'react';
import "./style.scss"
const SectionTitle = ({txt}) => {
    return (
        <>
            <h2 className='section-title'>{txt }</h2>
        </>
    );
};

export default SectionTitle;